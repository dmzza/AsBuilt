{
  "name": "asbuilt",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "Branching, partially-specified home modeling. M0: core language + solver spike.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.0",
    "vitest": "^2.0.0"
  }
}
