# AsBuilt Bugbot rules

## Review expectations

- Prefer concrete, reproducible bugs over style nits.
- When suggesting a fix, name the assertion a regression test should make (`src/**/*.test.ts(x)`).
- Agent/autofix test policy lives in `AGENTS.md` (Cloud Agents), not here.
